# Environment variables for acp-relay
# Copy this file to .env and fill in your values

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: Custom workspace directory for ACP agents (Docker mode)
# Default: ./data/workspaces
# This is the HOST path where agent workspaces will be stored
# ACP_WORKSPACES_DIR=/path/to/workspaces
